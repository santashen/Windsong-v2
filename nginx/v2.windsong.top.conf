# 宝塔 Nginx 配置
# 将此配置添加到站点 v2.windsong.top 中

server {
    listen 80;
    server_name v2.windsong.top;

    # 客户端最大请求体大小
    client_max_body_size 10M;

    # API 反向代理到 Go 后端（容器内 9080 端口）
    location /api/ {
        proxy_pass http://127.0.0.1:9080/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持（如果需要）
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 前端页面代理到 Vue 前端（容器内 9081 端口）
    location / {
        proxy_pass http://127.0.0.1:9081/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 健康检查（可选，用于监控）
    location = /health {
        access_log off;
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }
}

# HTTPS 配置（SSL 证书配置好后取消注释）
# server {
#     listen 443 ssl http2;
#     server_name v2.windsong.top;
#
#     # SSL 证书路径（宝塔申请的证书路径）
#     ssl_certificate /www/server/panel/vhost/cert/v2.windsong.top/fullchain.pem;
#     ssl_certificate_key /www/server/panel/vhost/cert/v2.windsong.top/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     client_max_body_size 10M;
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:9080/api/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location / {
#         proxy_pass http://127.0.0.1:9081/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# HTTP 重定向到 HTTPS
# server {
#     listen 80;
#     server_name v2.windsong.top;
#     return 301 https://$server_name$request_uri;
# }
